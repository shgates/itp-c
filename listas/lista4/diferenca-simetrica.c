#include <stdio.h>

void bubbleSortAndPrint(int a[], int n) 
{
    int temp;
    for(int i=0; i<n-1; i++)
    {
        for(int j=0; i<n - i - 1; j++)
        {
            if(a[j] > a[j+1]) 
            {
                temp = a[j+1];
                a[j+1] = 

            }
        }
    }
}

int main() {

    int n, m;
    
    scanf("%d %d", &n, &m);

    return 0;
}